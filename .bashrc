# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# User specific aliases and functions

alias g='git'
alias v='vim'
alias c='clear'
alias l='ls'
alias gc='gcc -g'
alias a='./a.out'
alias gd='gdb'
alias gda='gdb ./a.out'
